%html
  %head
    %title Virtual Assistant
  %body

    Welcome to Virtual Assistant

    - if current_user
      %h3 Here are your tasks:
      #peeps
      %ul
        - @tasks.each do |tee|
          %li
            %p
              Task:
              = tee.task
            %p
              Deadline:
              = tee.deadline.strftime("%d/%m/%Y")
            %p
              Location:
              = tee.location
            %p
              Tags:
              - tee.tags.map(&:tag_name).each do |x|
                %a{:href => "/tags/#{x}"}= x